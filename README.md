# Parent Project

This project vendors two child modules:
- `child-module-a` (vendored to `vendor/child_module_a`)
- `child-module-b` (vendored to `vendor/child_module_b`)

## Running

You can run the main script to see it in action:
```bash
python3 main.py
```
